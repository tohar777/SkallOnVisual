import js.Browser;
import Functions;
class Main {
    static function main() {
        //blocks
        /*
        text = TEXT
        blockinput = INPUT
        direct = ?? not in set yet!
        
        */
        var print = (function(){
             var blockJson = '
             {
                "color":"blue",
                "text":"print"
                "function":${}
             }
             ';
        })
        //blocks END
    }
}